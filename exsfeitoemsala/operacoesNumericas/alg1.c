#include<stdio.h>

int main(){

    //criar 3 variaveis para guardar numeros inteiros
    int a =0, b = 0, c = 0;
    scanf("%d %d", &a, &b);

    //somar os 2 números e guardá-los na terceira variável
    c = a + b;

    //imprimir na tela o valor da terceira variável
    printf("%d", c);

    return 0;
}